# 🚀 部署指南 - Streamlit Cloud

## 選項 A：自動部署到 Streamlit Cloud (推薦，最簡單)

### 前置條件
- ✅ GitHub 帳號
- ✅ Streamlit 帳號 (可用 GitHub 登入)

### 部署步驟

#### 1️⃣ 準備 GitHub 倉庫

```bash
# 創建 Git 倉庫
git init
git add .
git commit -m "🎨 Add PPT redesign Streamlit app"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/ppt-redesign
git push -u origin main
```

#### 2️⃣ 連接到 Streamlit Cloud

1. **訪問 Streamlit Cloud**
   - 開啟: https://share.streamlit.io
   - 用 GitHub 帳號登入

2. **創建新應用**
   - 點擊「New app」按鈕

3. **配置應用**
   - **Repository**: `YOUR_USERNAME/ppt-redesign`
   - **Branch**: `main`
   - **Main file path**: `streamlit_app.py`

4. **部署**
   - 點擊「Deploy」
   - Streamlit 會自動構建和部署

#### 3️⃣ 獲取應用連結

部署完成後，你的應用會在以下位置可用：

```
https://share.streamlit.io/YOUR_USERNAME/ppt-redesign
```

簡短連結：https://ppt-redesign-demo.streamlit.app (可自訂)

---

## 選項 B：本地運行

### 快速啟動 (Windows)

```bash
# 雙擊執行
run.bat
```

### 快速啟動 (macOS/Linux)

```bash
# 運行腳本
chmod +x run.sh
./run.sh
```

### 手動啟動

```bash
# 1. 安裝依賴
pip install -r requirements.txt

# 2. 啟動應用
streamlit run streamlit_app.py

# 3. 打開瀏覽器
# http://localhost:8501
```

---

## 選項 C：Docker 部署

### 創建 Docker 鏡像

```bash
# 使用提供的 Dockerfile
docker build -t ppt-redesign .

# 運行容器
docker run -p 8501:8501 ppt-redesign
```

### Docker Compose

```bash
docker-compose up
```

---

## 選項 D：其他雲端平台

### Heroku 部署

```bash
# 使用 Heroku CLI
heroku create ppt-redesign
git push heroku main
```

### AWS 部署

```bash
# 使用 AWS Elastic Beanstalk
eb init
eb create
eb deploy
```

### Google Cloud 部署

```bash
# 使用 Google Cloud Run
gcloud run deploy ppt-redesign --source .
```

---

## 🔧 配置說明

### 環境變數 (.env)

如果需要設置環境變數，創建 `.env` 檔案：

```
STREAMLIT_SERVER_PORT=8501
STREAMLIT_SERVER_HEADLESS=true
STREAMLIT_SERVER_RUNONSAVE=true
```

### Streamlit 配置 (.streamlit/config.toml)

```toml
[theme]
primaryColor = "#141E3C"
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#F0F0F0"
textColor = "#3C3C3C"
font = "sans serif"

[client]
showErrorDetails = true
toolbarMode = "viewer"

[logger]
level = "info"
```

---

## ✅ 檢查清單

部署前確保以下項目完成：

- [ ] `streamlit_app.py` 存在且無語法錯誤
- [ ] `requirements.txt` 包含所有依賴
- [ ] `.streamlit/config.toml` 已配置
- [ ] `README.md` 有完整說明
- [ ] `.gitignore` 已配置
- [ ] 所有範例 PPT 檔案已上傳

---

## 🐛 常見問題

### 部署失敗：ModuleNotFoundError

**原因**: 依賴未正確指定

**解決方案**:
```bash
# 更新 requirements.txt
pip freeze > requirements.txt

# 確保包含這些包:
# streamlit==1.28.1
# python-pptx==0.6.21
# pillow==10.1.0
```

### 應用啟動後無法上傳檔案

**原因**: 可能是檔案大小限制或權限問題

**解決方案**:
```toml
# .streamlit/config.toml
[client]
maxUploadSize = 200  # MB
```

### 配色未正確顯示

**原因**: 瀏覽器緩存

**解決方案**:
```bash
# 清除緩存並刷新
# Ctrl + Shift + Delete (Windows)
# Cmd + Shift + Delete (macOS)
```

---

## 📊 部署環境對比

| 平台 | 成本 | 易用度 | 可靠性 | 推薦度 |
|------|------|--------|--------|--------|
| **Streamlit Cloud** | 免費 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ✅ 推薦 |
| **Heroku** | 免費/付費 | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ |
| **Docker** | 自選 | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| **AWS** | 按需計費 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| **Google Cloud** | 按需計費 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |

---

## 🎯 推薦方案

### 對於課程作業：
```
使用 Streamlit Cloud (最簡單)
1. 上傳 GitHub
2. 連接 Streamlit
3. 自動部署完成
時間: 5 分鐘
```

### 對於個人使用：
```
本地運行 (最快)
執行 run.bat 或 run.sh
時間: 1 分鐘
```

### 對於企業應用：
```
AWS / Google Cloud (最穩定)
自動擴展、備份、監控
成本: $10-50/月
```

---

## 📞 部署支援

遇到問題？

1. **檢查日誌**
   ```bash
   streamlit run streamlit_app.py --logger.level=debug
   ```

2. **驗證依賴**
   ```bash
   pip check
   ```

3. **測試應用**
   ```bash
   python test_app.py
   ```

4. **查看文檔**
   - Streamlit: https://docs.streamlit.io
   - python-pptx: https://python-pptx.readthedocs.io

---

**🎉 部署成功！享受你的應用！**

